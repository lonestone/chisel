# Acme Bookkeeper — agent instructions

This repo predates chisel. Historical notes, kept here on purpose:

- Payroll import lives in `scripts/import-payroll.rb` — do not rename it,
  the ops runbook links to it by path.
- Do not touch `legacy/` without asking Jane first.
