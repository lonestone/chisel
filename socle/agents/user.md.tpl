# Personal agent settings

<!-- YOUR file, and yours alone. The chisel setup poses a copy of this
template at your first session and adds `.agents/user.md` to this project's
ignore rules: it is NEVER committed, `chisel update` never touches it, and
`chisel check` never looks at it. Your teammates have their own; some have
none at all, and that works — see below.

It answers ONE question: on YOUR tool and YOUR account, which concrete model
does each tier mean? The tiers themselves and the order in which this file is
read are NOT restated here — they are written once, in `.agents/methodology.md`,
section "Model tiers (and how they resolve)". Read that before editing this.

Everything below is commented out on purpose: an untouched copy of this file
changes NOTHING. Resolution simply falls through to the team default in
`.agents/project.md`, and then to the socle default, which every tool can
satisfy. Uncomment only what you actually want to override. -->

## Model tiers

<!-- Example. Uncomment the lines you want and replace each placeholder with a
model identifier your tool exposes. One line per tier; a tier you leave
commented out falls through to the next level of the cascade.

- **frontier:** <your tool's strongest reasoning model>
- **mid:** <your tool's standard everyday model>
- **cheap:** <your tool's fastest, least expensive model>

What each tier is for: `.agents/methodology.md`, same section. -->

## Notes for my sessions

<!-- Optional, and personal: anything you want every session of YOURS to know
and that no teammate needs — your OS quirks, your local ports, how you like
diffs presented, which shell you actually use.

Project-wide facts do NOT belong here: paths, gate commands, the tracker and
the reading list live in `.agents/project.md`, which everyone reads. A fact
written only here is a fact your teammates' agents will never see. -->
