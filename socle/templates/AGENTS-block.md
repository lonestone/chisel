<!--
  This file is the canonical SOURCE TEXT of the AGENTS.md managed block —
  not a substitution template; nothing is rendered into it. `chisel init`
  and `chisel update` copy it verbatim into your AGENTS.md, between an
  opening marker named `chisel:begin` and a closing marker named
  `chisel:end` (both single-line HTML comments in AGENTS.md).

  Ownership: everything BETWEEN those two markers is chisel-managed —
  `chisel update` overwrites that region on every run, so hand edits made
  there are silently lost at the next update. Everything OUTSIDE the
  markers — the rest of your AGENTS.md — is yours; `init` and `update`
  never touch it. To change what every project's block says, edit this
  file in the chisel socle (it is diffable and reviewed like any other
  source); to change it for one project only, edit outside the markers.
-->

Read `.agents/project.md` §C (the minimal reading list) at the start of a
session. Then:

- Creating a task → `.agents/rules/task-creation.md`
- Doing the work → `.agents/rules/task-progressing.md`
- Finishing a task → `.agents/rules/task-completion.md`
- Workflows, models, and available skills → `.agents/workflows.md`

This file only routes; the socle at `.agents/` carries the content.
